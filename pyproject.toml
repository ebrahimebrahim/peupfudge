[project]
name = "peupfudge"
version = "0.2.0"
description = "Build dependencies for the Peupfudge manual"
requires-python = ">=3.12,<3.13"
dependencies = [
    "matplotlib",
    "numpy",
]

[tool.uv]
package = false
python-preference = "only-managed"
